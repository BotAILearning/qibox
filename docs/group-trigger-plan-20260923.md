# 群聊触发规则调整计划（2026-09-23）

用户授权 Luna 6 开发，主代理负责飞牛真机 192.168.3.7 验收；通过后交付 0.8.9 fnOS 双架构包。用户选定实时周期 60 秒，授权验收群为「测试(3」，设备联系人目录中对应群名「测试」。

- @我开关开启：已验证的 @我进入必须回复路径；不得以无需回应 skip，不用固定承诺句替代生成。生成或读取失败显示明确错误。停止联系、人工接管、账号身份、取消、未知回执防重继续生效。
- @所有人开关开启：交给模型判断是否回复，允许 send/skip。
- 实时回复开启：每 60 秒汇总新消息给模型；无新消息不重复调用；连续新消息不能无限推迟计时。@我优先，同批消息只处理一次。
- 明确 @ 触发关闭时不能由实时开关绕过；仅 @ 他人不参与。
- 检查既有限流不静默消费必须回复消息，等待状态在重启后可恢复。
- 统一提示词、模型输入、输出协议和执行规则，更新用户文档及必要回归测试。

验收依次进行：本地针对性回归；主代理审查；真机备份、部署、源码哈希和服务状态核对；真机隔离回归与实际模型决策；授权群实际 @我、@所有人、60 秒普通消息收发与回执核对。未完成真实收发不能称全量验收通过，也不提前发布包。UGOS 仍暂停。

现有工作树包含其他已开发改动，禁止回滚或混淆来源；仅记录本轮变更与实际证据。设备当前版本 0.8.8-debug.001。

## 执行结果

Luna 6 开发、主代理复审和 SSH 真机验收均完成。详见 `reports/group-trigger-device-20260923/ACCEPTANCE.md`。用户另行授权的自动语音任务与本任务协调，按最后完成者统一打包约定，已将设备和最终发布交给该任务；本任务保持版本文件未修改，不重复打包。
