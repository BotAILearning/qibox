# Runtime slim acceptance record — 2026-09-25

## Baseline and target

- Source baseline: `a09a618771eb26d048d9c9527bf012e0cceec99b`, branch `codex/runtime-slim`, clean isolated worktree.
- Installed baseline: `0.9.8-debug.002`, 491,592,756 bytes, SHA-256 `74df4e3ee8954752855e9b66d5afc2827223c1b2f0f0b26cd5376a766cfcfd33` (handoff value; package hash was independently checked by the main task).
- Target: one `all.fpk`, product version `0.9.9`, build `0.9.9-debug.001`.
- Target device: `192.168.3.7`, read-only SSH confirmed Debian 12, x86_64, glibc 2.36, Node.js v22.18.0. Existing WeChat and private PulseAudio are running.
- Existing full verified upgrade backup is recorded in the prior acceptance record at `docs/AI-WIKI-ACTIVITY-ACCEPTANCE-20260924.md`; before the official install attempt, the exact path, COMPLETE marker and three archive hashes were revalidated. No new full chat-data archive was created.

## Change and component ledger

| Group | Decision | Evidence / boundary |
|---|---|---|
| Fcitx5, GTK/Qt frontends, libfcitx5, libime and pinyin data | Removed from x64 and ARM64 package locks; startup, required-start gate, environment, GTK module cache, configuration, and desktop-route coupling removed | 18 dedicated packages per architecture. Browser-native committed Unicode bridge already exists and prior real x64 WeChat input acceptance is recorded in project memory; this run still requires actual installed-WeChat input evidence before claiming the new package accepted. |
| Qt/WebEngine/QML/Quick and other Qt base packages | Retained on both architectures | x64 static WeChat scan found no Qt DT_NEEDED, but dynamic/plugin/subfeature validation is not complete; ARM64 has no device. |
| Debian FFmpeg/video codecs, GStreamer, DSP, PulseAudio utilities | Retained on both architectures | Existing WeChat maps official FFmpeg/VLC plugins; PulseAudio maps `libspeexdsp`; no complete deletion proof. |
| GTK, accessibility, DBus, X11, Python, CJK/Emoji fonts, private audio runtime | Retained | Required by current desktop, clipboard, file dialog, accessibility, and audio paths; device maps the packaged CJK and Emoji fonts. |
| PulseAudio library search path | Updated to include the architecture-matched private `usr/lib/<triple>/pulseaudio` directory | Pre-change device maps showed WeChat/wxplayer loading host `libpulsecommon-16.1.so` while the matching library exists in the bundled runtime. Confirm post-install `/proc/<pid>/maps`. |

## Input and clipboard checks

- `node --test test/native-input.test.mjs test/platform.test.mjs test/rfb-input-transport.test.mjs`: 21/21 passed. Input unit cases: 14/14 passed, including composition cancellation, default recovery storage on dispose, blur recovery, and clipboard-ready/RFB-key/next-clipboard ordering.
- `npm run check`: passed. `npm run build:web`: passed.
- First full run: 737 passed, 1 failed, 1 skipped. The failure was a Windows `EPERM` rename in an unrelated temporary-data test (`instances.test.mjs`); that file then passed 4/4 in isolation.
- Serialized full run: 737 passed, 1 failed, 1 skipped. The failure was another transient Windows `EPERM` rename in `ai-contact-learning.test.mjs`; that file then passed 24/24 in isolation. One UGOS platform test remains intentionally skipped.
- Latest serialized full run: 736 passed, 2 failed, 1 skipped (739 total); both failures were Windows `EPERM` atomic-renames in temporary test data (`ai-contact-learning.test.mjs` and `ai.test.mjs`). Re-running those two files together passed 62/62. One UGOS platform test remains intentionally skipped.
- The existing browser/RFB fixture script was attempted twice and failed in its setup before desktop input assertions (first waiting for the welcome heading, then for the fake package to report installed). It is not real OS IME or WeChat evidence in any case.
- Real Windows OS IME candidate selection in an actual WeChat text field: pending. Composition events generated by browser automation are not equivalent.
- Text, multiline, Emoji, file, and image paste into WeChat: pending.
- Required event order: clipboard owner ready → RFB Ctrl+V → next clipboard owner ready. Existing unit coverage checks ordering, but it does not prove the remote WeChat consumed the clipboard before a later owner replaced it; actual field readback is required.
- Recovery behavior for canceled composition, blur, disconnect, and dispose: pending actual browser/WeChat check; automated tests added for retained draft semantics.

## Runtime and product regression

- Clean-runtime absence of Fcitx files/processes: pending after build and installation.
- Runtime payload references and orphan-archive check: verifier strengthened; build result pending.
- x64 features: text, file, image, video, browser audio playback (distinguished from PCM stream), embedded web content, Wiki/activity/backup/AI and Bot autologin: pending post-install.
- ARM64: static archive, ELF and lock validation only; no ARM64 device acceptance. Uncertain Qt/media components retained.
- Prior known boundaries remain: old ~28-hour item failed exact location after 181 pages; native WeChat note write deferred; real reached reply-limit UI and desktop icon visibility lack production visual acceptance.

## Installation and Git delivery

- FPK: `dist/releases/0.9.9/0.9.9-debug.001/qibox-0.9.9-debug.001-all.fpk`, 440,610,224 bytes, SHA-256 `91a036bf186f04e35f29a708fededf64888420a0ca2e56dffde08c8f55bbc03d`. This is 50,982,532 bytes (10.37%) smaller than the supplied baseline.
- `scripts/verify-fpk.py` passed after adding equality between actual packaged runtime archives and the unique archive references from both architecture locks. It checks every referenced archive hash; this run also confirms no unreferenced payload archive remained. Package file parity, bundled-font hashes, and UI/source parity passed.
- FPK checksum was re-read after the install attempt and remains SHA-256 `91a036bf186f04e35f29a708fededf64888420a0ca2e56dffde08c8f55bbc03d`, size 440,610,224 bytes. `verify-fpk.py` reports independent identity/version, Unix mode and component-hash checks passed, confirms no bundled WeChat, no private-key material, and no archive paths for local credentials, reports, caches, backups, or test/build output. The supported production `node_modules/ws` dependency remains packaged.
- Official App Center upgrade on existing data volume and Bot instance startup: pending.
- Existing verified backup was rechecked before install: COMPLETE marker and hashes of all three archives matched its manifest (including the 52.9 GB data archive). No additional chat-data backup was created.
- A direct invocation of the official root-owned `appcenter-cli install-fpk` returned `[Info]Application [qibox] is installed.` but performed no upgrade, matching the repository's documented fnOS CLI behavior (`docs/LOCAL-FILES.md`: for an existing app, this command only reports installed). Read-only checks immediately afterward still show product/build `0.9.8-debug.002`, manifest `0.9.8`, old runtime fingerprint, and bundled Fcitx present. Therefore this is not a successful installation of 0.9.9 and no hot-deploy/manual overwrite was attempted.
- The pre-existing Bot instance remains on its original saved identity, running and logged in; its contacts (1,936), profiles (1,608) and memory entries (133) match pre-install counts. The saved test identity also matches; its runtime reports running and login status unknown. No chats/messages/settings or user data were intentionally changed.
- Official App Center GUI/API upgrade, 0.9.9 background Bot startup, post-upgrade PulseAudio mapping, and real feature/input evidence: not verified. Computer-use interaction was stopped because Chrome's current URL could not be established with sufficient confidence; the authorized UI install route is consequently unavailable in this run.
- Source commit was pushed to `codex/runtime-slim` and its remote head was verified against the local commit. Official App Center upgrade and the associated installed-version verification remain blocked/pending as described above.

## Tooling boundary

The computer-use skill stopped browser interaction because it could not establish Chrome's current URL with sufficient confidence. No UI typing or sending was performed through that tool. This does not substitute for actual Windows IME or media playback acceptance; report the boundary explicitly unless another supported, policy-compliant browser surface becomes available.
