# AI 回复设置与群聊上限真机验收（0.9.21-debug.001）

## 交付内容

- 调整联系人与群聊的回复设置布局、聊天记忆标题与分类、回复策略及操作区；连续自动回复上限使用预设下拉与手动输入，文案为「不限」，手动最大 99999。
- 超出手动上限时给出两个选择。「好的，明白」保留当前输入；选择「不限」只改变页面草稿，点击「保存设置」后才生效。
- 群聊提及回复的次数上限仅统计 @我和 @所有人；AI 实时回复独立显示，并提供正常回复、积极主动两种参与方式。

## 本地检查与包

- `npm test`：746 通过、0 失败、1 项既有 UGOS 环境用例跳过；`npm run check`、`npm run test:ai-ui` 与本轮联系人/群聊浏览器回归通过。
- fnOS 双架构包：`dist/releases/0.9.21/0.9.21-debug.001/qibox-0.9.21-debug.001-all.fpk`，491608496 字节；SHA-256 `0ac314f06a860961aa8028ea0f3829b3e3a3aaff5a8c05c772f3e342b2919dd7`。1132 个包内文件与构建输入逐项校验一致；不包含微信程序，未制作 UGOS 包。

## fnOS 真机检查

- 设备 `192.168.3.7`。安装前同步本轮模块到真机，复测发现重复输入新的超限数字时弹窗不再出现，已修复并重新验证。
- 真机页面：联系人自动回复关闭时三个子项仍显示；群聊 @我、@所有人、上限同排，实时回复及两种参与方式位于下方；超限弹窗两个选项及未保存不生效均通过；390、768、1024、1440 像素无整页横向溢出。
- 受控群「测试」暂将上限 50 改为 1：第一条 @Bot 消息产生真实 AI 回复，群聊提及计数 0→1；第二条 @Bot 消息产生上限事件，没有继续回复，也没有暂停群聊。随后一条普通群消息进入实时回复判断，模型选择跳过，说明提及次数上限未阻断实时回复。测试结束将上限恢复为 50，群聊开关保持原状态。
- 包的设备 SHA-256 与本地一致。应用中心 `update/info` 确认目标为已安装的 qibox、原存储卷 1、无新增安装表单；正式升级任务 `1790366466545677725-qibox` 返回 `status=2, progress=100`。登记版本为 0.9.21，运行构建号为 0.9.21-debug.001；7 个关键服务、页面和配置文件的 SHA-256 与包内构建输入一致。升级前后 `users.json` SHA-256 一致。
- 安装后重复真机页面验收并通过。Bot 微信运行中且已登录，AI 接口可用。受控测试账号再次登录并发送 @Bot 消息，Bot 产生真实回复，群聊回复轮数 6→7、提及计数 0→1，发送状态为 `sent`；接收端页面可见回复。测试账号随后恢复为原先的停止状态，Bot 保持在线，测试群上限仍为 50。
- 用户在升级前要求停止完整备份，已停止复制并清理本轮未完成的备份目录；仅保留新 FPK 与升级任务记录。没有将部分备份写成完整备份。ARM64 尚未在对应硬件上安装验收。

受控测试截图与详细状态保存在本地忽略目录 `reports/real-device-ai-reply-20260926/`，不进入安装包或 Git；本报告不含模型密钥、聊天正文或数据库密钥。
