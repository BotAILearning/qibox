# AI 回复记录与接续改进验收

日期：2026-09-24  
版本：0.8.15-debug.003  
范围：fnOS x86_64；UGOS 开发与打包暂停。

## 改动

- 系统设置中接续方式与 AI 辅助等待时间并排显示，窄屏换行。
- 模型卡片编辑时左侧用途保持已保存值，右侧编辑值仅在保存成功且请求上下文仍有效时提交；失败、取消或切换时保留草稿。
- 已代发自动回复提供删除记录入口；删除校验当前微信账号及记录来源。未回复记录可删除并标记为需回复。
- 已回复记录按本次接管、全部、近一天、近一周、近一月进行联系人范围总结，返回覆盖时间、消息数量、AI 回复数量和截断状态。首次由 AI 接管时使用该联系人的回复监控/配置/准备时间作为起点，最后回退到最早可确认的 AI 回复时间；没有起点时回退到当前时间。
- 标记待回复内容按账号、联系人和消息持久化加密暂存。下一次该联系人的自动回复尝试先总结，成功后将摘要写入该联系人的回复上下文并清除本次原文及待处理项；失败保留待处理项且不会无限阻止本轮正常回复，后续自动回复重试。输入长度受限，模型提示明确把聊天内容视为引用资料。
- 未回复定位授权精确核验当前账号、联系人及跳过事件中的 messageId；不存在或跨联系人的记录会被拒绝。原生定位按数据库确认的目标方向、唯一连续上下文锚点和可识别媒体间隔匹配；未知方向仅在文本序列唯一且存在已确认方向锚点时允许。

## 本地验证

- `npm test`：714 项，713 通过、0 失败、1 项 UGOS 专属环境检查跳过。
- `npm run test:ai-ui`：通过。
- `npm run check`：通过。
- `node --check`：`server/index.mjs`、`server/ai-service.mjs`、`web/ai-assistant.mjs`、`web/ai-activity-view.mjs` 通过。
- 新增回归覆盖模型保存提交状态、已回复删除入口、总结筛选与 AI 回复归属、首次 AI 接管、联系人隔离、定位权限、待回复数据加密持久化、总结成功清理、失败保留和后续重试、总结失败不阻断正常自动回复。
- `npm run check`：通过；`npm run test:ai-ui`：8 项浏览器回归通过；Python 原生定位测试 6/6 通过；`git diff --check` 通过。

## fnOS 真机

- 目标：`192.168.3.7`，fnOS x86_64。改动前保存 server/public 文件备份：`/vol1/@appdata/qibox/backups/pre-0.8.14-20260924/`；产品配置另存为 `/vol1/@appdata/qibox/backups/pre-0.8.14-20260924/product-before-0.8.15.json`。没有清理或覆盖用户聊天、联系人或 AI 设置。
- 备份后热同步本次构建文件；`/api/session` 返回版本 0.8.15、host fnos、consent accepted。qibox 服务与 WeChat 进程运行，实例状态 running。
- 首页、app.js、ai-workspace.css 返回 HTTP 200；同步文件 SHA-256 与本地候选构建一致。新接口对随机不存在消息/事件 ID 返回预期 409：定位为“该消息不属于当前联系人的执行记录”，标记为“未回复记录已变化，请刷新后重试”。这两项拒绝测试未保存状态。
- uid 1001 的实例没有模型，uid 1000 的实例已有 2 个模型和已同意的会话。只读识别到唯一测试群联系人后，在近一天范围实际总结 5 条消息（含 2 条 AI 回复），模型返回有效总结；日志只保存范围和计数，不保存摘要或聊天正文。
- 测试群已有 3 条 skip 记录但均无 messageId，因此无法用它们做定位或标记。另从 uid 1000 选择账号、profile、contact 均匹配且有 messageId 的 skip 记录：只读 review 返回历史并包含目标 ID。`.003` 真机请求返回 `located=true` 且 messageId 与授权记录一致；对另一联系人提交同一 messageId 返回 HTTP 409。定位序列验证跳过一条已识别媒体消息，仍要求唯一锚点及连续普通聊天行。没有向联系人发送消息或伪造聊天记录。FPK 尚未通过应用中心安装。
- 标记→自动回复的真实模型成功链路已在用户确认的 Bot 与受控测试账号一对一联系人中完成真机验证；完整链路、清理及设备恢复证据见下方“#5 真机端到端补充”。

## FPK

- 路径：`dist/releases/0.8.15/0.8.15-debug.003/qibox-0.8.15-debug.003-all.fpk`
- 大小：491576947 字节；SHA-256：`e1f99f0a4ff2082ffe9bcf60845dded1f05f4e660ea6a8d6cc78f5b90626353b`。
- `scripts/verify-fpk.py`：1132 个包内文件及身份、版本、权限、组件 hash 校验通过，未捆绑 WeChat。
- 未公开发布。

## #5 真机端到端补充（2026-09-24）

- 在用户确认的 Bot ↔ “毒药💊”受控一对一联系人中进行真实微信来信测试，未向其他联系人发送消息。第一条来信产生带 messageId 的真实未回复 skip；标记为需回复后，目标联系人的持久化待处理数为 1。
- 第二条真实来信触发自动回复调用前的总结。模型总结成功后，目标联系人待处理数从 1 降为 0，专属 replySummaryContext 新增本轮内容；队列空闲、无错误。两条消息均明确要求不回复，因此本轮未观察到 AI 发出消息。未伪造事件或 skip。
- 已通过 API 恢复该联系人原选定状态（enabled=false、multiTurn=false、judgeReply=true），replyTargets 恢复为原 15 个对象；无其他联系人被更改。本轮 skip/pending 已清除。
- 清理完成：先备份两端 AI 状态、PID 与 session-resume 文件，再以官方生命周期脚本受控停止 qibox；只对目标 profile 的 replySummaryContext 单字段做原子恢复。目标摘要上下文与测试前备份一致，本轮唯一标记计数为 0，pending 与本轮 skip 均为 0；启动后 API 缓存和磁盘状态一致，replyTargets 为原 15 个且列表/目标 replyOptions 与基线一致。
- 停启后 Bot 自动恢复登录；test 的 session-resume 曾记为未登录，因此用其唯一 WeChat PID 的原生辅助程序执行单次安全登录按钮检测/点击（仅唯一登录按钮，不扫码、不触碰会话内容），随后 API recheck 确认 running、logged-in、canConnect=true。Bot 同样为 running/logged-in；两个 AI 队列 idle，app.sock 与官方生命周期 status 均正常。
- 两条真实测试来信保留在受控 Bot 与毒药💊私聊历史中；未删除原有聊天、联系人、其他对象或配置，也未向其他联系人发送消息。
