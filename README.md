# 栖盒

当前开发构建：**0.8.0-debug.001**。版本与构建记录见 [版本记录](docs/release/VERSION-CONTROL.md)。

**平台计划（2026-09-15）：绿联 UGOS 安装包暂停开发与打包，移出当前必须交付范围；保留已有源码和产物，等待用户明确通知恢复。**

项目级协作原则见 [协作规则](docs/PROJECT-COLLABORATION-RULES.md)。讨论结论、用户问题和待执行验收用例统一见 [开发测试流程](docs/DEVELOPMENT-TEST-FLOW.md)；2026-09-15 的核对与设计建议见 [问题核对报告](reports/review-2026-09-15/VERIFICATION.md)。登记不代表已实现或已通过实机验收。

## 历史版本说明

0.1.32 修复原生发送校验依赖气泡颜色、资料面板清理超时的问题；由界面核验联系人并提交文字，必须收到数据库新增消息才确认发送。包含 0.1.31 的自动回复名称、搜索、开关保持和回复进度修复。

0.1.31 修复刷新联系人、保存策略后 AI 总开关被关闭的问题，重启后保留开关并重新核验当前账号；通过稳定消息标识保存回复进度，处理策略生效后在首次轮询前收到的消息，避免漏回和重复回复。回复功能入口改为“自动回复”，联系人列表支持名称搜索。

0.1.30 修复 Linux 微信 4.1.13.9 的通讯录读取失败：兼容编码后的数据库密钥缓存，按结构定位并通过数据库认证，不再依赖内存中的明文密钥字符串。联系人按好友类型筛选并排除已删除条目。已在指定飞牛 x86_64 设备的正式 API 读取到 49 位好友，并验证测试联系人的历史消息解析；详情见 [实机修复记录](reports/CONTACTS-DATA-0.1.30.md)。

0.1.29 将 AI 联系人和聊天读取切换为实例私有的本地数据接口，不再逐项打开通讯录或翻动聊天窗口。按需读取加密数据页并解析已提交的近期消息，所选聊天沿用现有模型学习流程。修复短暂登录检测失败和状态请求失败导致 AI 入口消失、配置面板被重置的问题。新数据适配已通过本地加密数据库与浏览器回归，NAS 实机读取权限、微信版本兼容性和真实模型仍待安装后验证，详见 [验证记录](reports/AI-DATA-0.1.29.md) 和 [本地数据接口说明](docs/WECHAT-DATA.md)。以下版本记录中的界面扫描与读取范围描述属于旧版本。

0.1.28 取消闲时备份资格检测，直接设置每日时段；闲时启动后自动点击微信启动页的“登录”。前期仍需扫码，后续勾选“自动登录该设备”后才可自动登录。AI 辅助仅在微信登录成功且桌面已连接时显示。详见 [本次调整](reports/IDLE-0.1.28.md)。

0.1.27 将 AI 辅助入口调整为微信登录后显示，退出登录或连接断开时隐藏；修复联系人扫描中的账号设置窗口识别、通讯录分页及身份读取问题。详见 [修复记录](reports/CONTACTS-FIX-0.1.27.md)。

0.1.25 将回复策略与主动聊天统一从联系人列表开始：回复策略逐人展示，未设置保持空白，可手动选择策略或单独学习聊天风格；批量学习每次最多 10 人，聊天按联系人整合后只调用一次模型。移除标签分组及按风格展示，主动聊天保留明确勾选的联系人并按配置发起。联系人发现不再要求当前已打开聊天窗口。详见 [AI 辅助说明](docs/AI-ASSISTANT.md)。

0.1.24 完善 AI 辅助：API Key 保存后保留星号，可展示或隐藏；回复策略新增多轮问答，按需分段、随机等待及追问；主动聊天可围绕目标持续进行多轮沟通。学习时明确分析用户对选定联系人的表达风格。详见 [AI 辅助说明](docs/AI-ASSISTANT.md)。

0.1.23 重排 AI 辅助：模型设置移到右上角，主页提供主动聊天与回复策略开关；学习所选聊天后按风格或联系人展示模型结果，再确认应用。主动聊天改为五步配置，支持手动、粘贴学习和引用已有风格，可分段发起并围绕本轮目标接续。详见 [AI 辅助说明](docs/AI-ASSISTANT.md)。

0.1.22 修复微信自动登录检测读取已关闭设置窗口、检测刷新时闲时备份反复置灰的问题；修复 Anthropic 服务地址缺少 `/v1` 导致连接失败，新增 MiniMax 预设。使用提供的 MiniMax-M3 配置已通过真实连接测试。验证范围与安装包状态见 [0.1.22 修复记录](reports/BUGFIX-0.1.22.md)。

0.1.21 优化模型与策略配置：提供常用服务预设、自定义接口、模型列表拉取与当前配置测试；主动目标提供六类可编辑模板；识别需要文件或通话能力的文字请求时，暂停对应对象并提示手动处理。源码与网页已完成验证，打包与交付状态见 [0.1.21 验证记录](reports/AI-ASSISTANT-0.1.21.md)。

新增 AI 辅助入口与总开关，位于微信桌面左侧，全屏下也可使用。支持自带模型、策略配置、风格学习与编辑、辅助对象选择、主动联系队列和自动回复开关。主动联系按每次随机 2–5 分钟依次执行。用户操作微信时临时让出桌面，检测到手动回复后暂停该对象的自动回复。

AI 辅助保持官方微信程序原样，只通过实例私有桌面的外部接口操作。聊天正文不由 AI 辅助持久化；模型密钥加密保存，策略、风格和不含正文的队列状态按实例隔离。关闭、取消及学习结束会清理内存上下文，用户输入桌面前会先停止原生自动操作并完成交接。功能约定和微信适配边界见 [AI-ASSISTANT.md](docs/AI-ASSISTANT.md)。已在指定飞牛 x86_64 设备验证单聊身份、仅限指定对象的扫描、连续文字读取与一条测试消息的发送回执。当前适配已测浅色文字布局，群聊、媒体和未知布局不自动处理。模型服务由用户部署后自行配置；真实模型、多对象和完整自动聊天流程尚未验收。

0.1.20 最终 FPK 已生成并通过包校验。NAS 部署尝试失败，官方本地安装命令卸载旧应用后安装未成功，原数据目录已不在原路径，恢复情况未确认；用户已接手安装。详情见 [0.1.20 开发记录](reports/AI-ASSISTANT-0.1.20.md)。

绿联 ARM64 0.1.15 修复 UGOS 网关未传入用户类型时，有效登录被错误拒绝、精选应用与桌面持续空白的问题。已在 DH4300 Plus / UGOS 1.19.1 上复现。用户类型缺失时提供普通用户功能，卸载共用程序仍要求明确的管理员身份。同时兼容非数字用户 ID，保留首次连接重试和最新透明底图标；原数字账号数据目录不变。

沿用 0.1.10 的图标缓存更新方式：桌面图标文件名和网页图标地址随图片内容变化。升级后重新加载飞牛页面以加载新入口。

新增彩色表情、装饰与数学符号字体，修复昵称及文字中部分图案显示为方框的问题。栖盒图标为透明背景的绿色开口盒子。字体支持范围及验证见 docs/FONTS.md。

精选应用在左，桌面在右。左侧采用紧凑卡片。闲时备份无需检测即可设置。真实自动登录完整周期仍待验收，详见 docs/IDLE-POLICY.md。

在自己的 NAS 上运行官方 Linux 微信，把微信实际接收到的记录保存在 NAS，并选择手动启动、持续备份或闲时备份。飞牛使用独立应用标识 qibox，入口 /app/qibox/；绿联原生 UPK 使用 com.bot.qibox。均使用系统分配的应用数据目录，与其他项目没有数据连接。

## 使用

- 首次下载安装微信，或导入与设备匹配的官方 x86_64 / ARM64 .deb 包。页面自动提示当前设备格式，下载自动选择架构。当前可信导入版本为 4.1.13.9，具体 SHA-256 见 config/wechat.json。安装一次可多开。
- 下载按 NAS 实际接收字节显示百分比、大小和近期速率；无法取得总大小时仅显示已下载大小与速率。安装按准备、校验、检查、解压、完成分阶段显示，校验和解压分别按实际处理字节计算，其他阶段显示处理中。
- 打开微信并完成官方登录，记录由微信保存在 NAS。栖盒不另存快照、不自动导入手机全部历史记录，也不将进程运行状态当作微信已登录或备份完成。
- 手机仅显示下载安装和已安装微信入口；打开时提示使用电脑，不启动或连接微信。
- 电脑左侧精选应用负责安装、添加到桌面、恢复和卸载；右侧桌面点击应用图标打开，支持键盘 Enter。启动设置、重命名、停止、删除收在图标菜单。手机保持仅安装页面。
- 添加、重命名和恢复统一校验名称，使用中及保留数据的应用都不能重名；比较前去掉首尾空格，名称占用限于当前 NAS 用户。添加时默认提供未占用的名称。恢复旧数据遇到重名时，需修改名称后恢复，原实例 ID 和数据目录保持不变。

## 保留与删除

删除应用时默认保留数据。再次使用时从“保留的数据 → 恢复使用”选择原应用，再登录原微信。恢复复用原实例 ID、HOME、登录文件及聊天数据目录；不会凭微信账号自动匹配一个新建实例。存在保留数据时，在应用市场添加时会先提供恢复选择，仍可选择新建。

永久删除要求输入“确认删除”加应用完整名称，例如名称为“工作微信”，需输入“确认删除工作微信”。删除保留数据适用相同规则。

“卸载微信”移除共用微信程序和安装包，默认保留数据，重装后继续使用原目录。也可选择清除当前操作者名下全部微信数据，包含保留的数据，需输入“确认删除微信”。卸载先停止所有使用共用程序的微信，停止失败则不移除程序。入口限 NAS 管理员；清除数据始终限制在请求者自己的数据目录，不删除其他用户的数据。

## 启动设置

- 手动启动：需要时打开。关闭网页不停止微信。
- 持续备份：保存设置后立即检查启动，此后每 30 秒检查进程。登录后接收并保存记录。与电脑登录同一微信冲突，适合主要使用手机时开启。失败尝试间隔至少 60 秒，同一天连续 3 次失败后停止重试；主动停止会持续暂停，直到再次打开或保存设置。
- 闲时备份：按每日时段启动和退出（北京时间，支持跨夜），不再检测资格。前期需扫码；微信允许并勾选“自动登录该设备”后可在定时重启后自动登录。定时启动时栖盒尝试点击启动页“登录”，仍需扫码或手机确认时由用户完成；主动打开接管的会话不被计划强制关闭。

微信按账号在设备上的登录情况决定自动登录资格。栖盒读取各实例当前账号的原生决定，不把某台 NAS 的检测或进程运行七天视为所有账号已获准。凭据绑定账号摘要、设备摘要、实例和配置；切换账号、搬到其他设备或实例后重新判断。缺少可靠检测结果时保持锁定，旧手动确认不再有效。核对进度和边界见 docs/IDLE-POLICY.md。

## 应用目录

server/catalog.mjs 定义市场条目，桌面及保留数据保存 appId，旧数据默认归属 wechat。安装接口按 /api/apps/:appId/install/* 分派；保留旧安装接口兼容已打开的旧页面。当前只上架官方微信，不接受未适配的应用标识。后续接入新应用时仍需实现其安装库、运行器和设置策略；不是任意 Linux 安装包通用安装器。

## 构建与验证

需要 Node.js 22、Python 3 和 fnpack。运行依赖由 config/runtime-lock.json 和 config/runtime-lock-arm64.json 固定，缓存分别位于 .cache/runtime 和 .cache/runtime-arm64。微信不打进栖盒安装包。

```
npm ci
npm run prepare:runtime
node scripts/prepare-runtime.mjs --arch=arm64
npm run build:web
npm test
npm run check
npm run test:ui
npm run test:desktop
npm run build
```

上述为当前飞牛构建与验证入口。绿联构建脚本及历史说明保留在项目中，暂停期间不执行 UPK 构建。

工具路径可由 QIBOX_BUILD_PYTHON 和 QIBOX_PLAYWRIGHT 指定。npm run dev 提供本地页面预览，实际微信须安装到 NAS 使用。

飞牛统一包的输出路径为 `dist/qibox-<产品版本>-all.fpk`，包含 x86_64 和 ARM64 运行组件，依赖对应架构的飞牛 Node.js 22。启动只解压当前架构，新增字体由两种架构共用；打包支持双架构不代表 AI 原生适配已在 ARM64 实机验收。绿联按架构输出原生 UPK，内置对应架构 Node.js 22，需 UGOS Pro 1.13.0.0000 或更新版本及测试设备开发者授权。构建核对版本、入口、脚本权限、字体和全部组件哈希。平台差异、构建及安装步骤见 docs/MULTIPLATFORM.md；验收记录见 reports/RELEASE.md。

开源事项见 NOTICE.md。

发布者 Bot；联系邮箱 lbp240lbp@163.com。
