# AI 辅助 · 蓝紫方案设计核对

日期：2026-09-15。范围：本地交互设计预览；未修改正式 web/server 源码，未打包或部署。

## 视觉依据

- 用户提供的影视墙截图，原始尺寸 2400 × 1342；仅提取配色、左侧导航和卡片语言，不把影视分类、影片及截图内说明当作产品需求。
- 最终在同一工具输入中展示原图与 `screenshots/contact-reference-size.png`（2400 × 1342）进行对照。
- 另检查 1440 × 960 桌面、390 × 844 窄屏；保存按钮在桌面首屏可见，窄屏纵向滚动可到达。

## 发现与修复

- P2 已修复：初版 1440 × 960 首屏的保存区域偏低。压缩顶栏、标题区和分组留白，复看后保存按钮完整可见。
- P2 已修复：辅助说明初版为 10px 且偏浅；关键说明提高到 12px 并加深颜色，表单与开关文案提高到 14px。
- P2 已修复：弹窗初版没有键盘焦点管理。增加首次聚焦、Tab 循环、Escape 退出及关闭后焦点恢复；浏览器观察到打开学习弹窗时焦点落在范围选择器。
- 截图工具的 fullPage 输出出现缩放和拼接异常；最终交付截图改用当前视口截图，异常截图不作为验收依据。

## 五项视觉检查

| 项目 | 结果 |
| --- | --- |
| 字体 | 系统无衬线及 Microsoft YaHei 回退；标题、表单、辅助说明层次明确。不是影视墙字号的逐像素复制。 |
| 布局 | 深色左栏约占宽屏 18%；主体浅底、独立白卡、16–18px 圆角。将大幅影片展示区缩为标题区，给 AI 设置更多空间。 |
| 颜色 | 左栏 #202231、主色 #4e64ff、背景 #f5f6fb、白卡 #ffffff、边框 #e6e8f2、浅紫学习按钮。 |
| 图像图标 | 使用 Phosphor 正版图标库；联系人首字为动态名称标识。未引入电影封面或装饰图形，深色标题卡是实色 UI 容器。 |
| 文案 | 页面为 AI 自动回复、主动聊天、运行记录、模型设置；示例数据与模型模拟反馈有明确标识。 |

## 已操作验证

1. 四页导航、联系人与群聊切换；联系人首屏配置及群回复开关展示。
2. 自动回复关闭后两个子开关禁用，重新开启恢复原值；保存产生本地反馈。
3. 群实时回复开关先弹出确认，取消后保持关闭。
4. 学习弹窗打开及应用示例风格，选择器显示“已学习的本人风格”。
5. 主动聊天填写目标、选择对象、启动示例任务并暂停。
6. 模型表单使用测试文本提交，运行记录显示保存和示例任务事件。
7. 390px 窄屏可进入详情，内容宽度与视口同为 390px，无横向溢出；保存按钮可见于下半屏。
8. 浏览器未捕获 error 级别日志；构建通过。

## 范围与后续

此为方案预览，不代表真实微信、模型服务或 NAS 验收。正式采用时须沿用现有产品的模型预设、草稿、发送状态、主动聊天完整策略步骤、筛选与接管能力，不能用本预览的简化逻辑替代。

final result: passed
